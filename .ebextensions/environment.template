option_settings:
  aws:elasticbeanstalk:application:environment:
    ENV: ${ENV}
    DEBUG: ${DEBUG}
    LOGGING: ${LOGGING}
    RESN_USERNAME: ${RESN_USERNAME}
    RESN_PASSWORD: ${RESN_PASSWORD}
    RESIN_APP_ID: ${RESIN_APP_ID}
    BITBUCKET_PASSWORD: ${BITBUCKET_PASSWORD}
    GITHUB_ACCESS_TOKEN: ${GITHUB_ACCESS_TOKEN}
    PAYPAL_CLIENT_ID: ${PAYPAL_CLIENT_ID}
    PAYPAL_CLIENT_SECRET: ${PAYPAL_CLIENT_SECRET}
    PAYPAL_BILLING_PLAN_TRIAL: ${PAYPAL_BILLING_PLAN_TRIAL}
    PAYPAL_BILLING_PLAN_REGULAR: ${PAYPAL_BILLING_PLAN_REGULAR}
    PAYPAL_BASE_URL: ${PAYPAL_BASE_URL}
    PAYPAL_WEBHOOK_ID: ${PAYPAL_WEBHOOK_ID}
    BLOCKCYPHER_WEBHOOK_TOKEN: ${BLOCKCYPHER_WEBHOOK_TOKEN}
    BLOCKCYPHER_API_TOKEN: ${BLOCKCYPHER_API_TOKEN}
    BLOCKCYPHER_WALLET_NAME: ${BLOCKCYPHER_WALLET_NAME}
    BLOCKCYPHER_COIN_SYMBOL: ${BLOCKCYPHER_COIN_SYMBOL}
    BITCOIN_PAYMENT_WALLET_XPUBKEY: ${BITCOIN_PAYMENT_WALLET_XPUBKEY}
    SMTP_USERNAME: ${SMTP_USERNAME}
    SMTP_PASSWORD: ${SMTP_PASSWORD}
    SMTP_FROM: ${SMTP_USERNAME}
    SMTP_RCPT_TO: ${SMTP_RCPT_TO}
    GEOIP_API_KEY: ${GEOIP_API_KEY}
    PURGE: ${PURGE}
    PURGE_WHITELIST: ${PURGE_WHITELIST}
    MAX_LAST_SEEN_DAYS: ${MAX_LAST_SEEN_DAYS}
    API_SECRET: ${API_SECRET}
    BOOTSTRAP_CSS: ${BOOTSTRAP_CSS}
